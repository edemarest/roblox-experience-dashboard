export default function AdminViewer(){
  return (
    <div style={{padding:20}}>
      <h2>Table viewer</h2>
      <p>This is a lightweight table viewer placeholder. Use the API endpoints to query /api/v1/admin/table/:name?limit=100</p>
    </div>
  );
}
