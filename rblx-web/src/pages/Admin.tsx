import AdminDashboardMock from './AdminDashboardMock';

export default function AdminPage() {
  return (
    <main>
      <h1>Admin / Debug</h1>
      <p>Admin tools and debug pages go here (migrations, jobs, run-once tools).</p>
      <AdminDashboardMock />
    </main>
  );
}
